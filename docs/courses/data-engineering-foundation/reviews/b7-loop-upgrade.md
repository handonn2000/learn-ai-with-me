# DEF Week 7 — repeating visual revision

Completed 2026-09-09, following the user's request for automatic repeating explanations,
use-case tabs, varied visual styles, authentic framework icons, themed code and emphasis.
This report supersedes the paused-player behavior in the earlier visual-upgrade report.

## Delivered

Seven explanatory figures and four lab scene clocks repeat automatically. Playback,
next/back, speed, reset, send and read buttons were removed from these explanations.
Scenario tabs compare storage layouts, Kafka ecosystem paths, routing cases, eight crash
combinations, CDC methods and retention policies. Assessment controls remain functional.

Representations include a transit path, moving storage tiles, branching SVG signals,
round-trip processing, isometric cluster transfer, offset conveyor, CSS 3D broker racks,
event postcards, receipt/commit filmstrip and archive pruning. The nine authentic local
framework logos and their documented provenance remain in use.

JSON has escaped, selectable syntax tokens for keys, strings, numbers, literals and
punctuation in both themes. Ten chapter callouts and selective term emphasis clarify
important conditions and conclusions. Equivalent updated VI/EN instructions describe
loops and scenario tabs. Existing engine, source-detail tables, quiz, route and progress
keys remain unchanged by this visual revision.

Visible foreground scenes own their clocks; offscreen scenes pause and unmount cleans up.
Reduced motion presents complete static outcomes with persistent explanations/tables.
Tabs support arrow keys and Home/End. Frames are not live-region announcements.

## Verification

- Production TypeScript/Vite build: PASS, re-run on 2026-09-09.
- `node scripts/check-def-b7.mjs`: all five groups PASS (crash cases, routing/independent
  groups, CDC/retention, locale/quiz structure, course storage/route wiring).
- `python3 .claude/scripts/check_i18n.py def-b7`: PASS, 726 paired strings, 199 keys,
  six clean renderer files, all 299 baseline strings accounted for. Fifteen intentionally
  edited near-match warnings remain; they are not extraction losses.
- Browser checks: all eleven default scene clocks traversed every phase and at least
  two reset boundaries using a controlled browser clock. Journey also advanced in real
  time. No explanatory transport controls, selects or frame live regions. All scenario
  outcomes, keyboard tabs, JSON copy validity and logo loads passed.
- VI at 375px and 320px: no document/figure/lab/JSON overflow. Light/dark screenshots,
  static reduced motion, actual CSS 3D, scenario variants and chapter code were inspected.
- Offscreen clock suspension, cleanup on navigation and hidden deck scenes passed.
  Document-hidden behavior used a synthetic visibility event; this is not a claim of
  testing an actual OS background tab. No browser errors in the principal suite.
- Both modified lesson skill entrypoints pass `quick_validate.py`; `git diff --check` passes.

Machine-readable check scope and final source hashes: `b7-loop-browser-checks.json`.
Browser screenshots and temporary harnesses are local session artifacts under
`/tmp/def-loop-check`, `/tmp/def-loop-browser.cjs` and `/tmp/def-loop-views.cjs`.

## Bilingual baseline audit

Only two obsolete baseline instructions were intentionally replaced to reflect the
user-authorized UI change: the chapter 7 action card now describes the excess-consumer
tab and independent reader progress; the old undo/reset routing note now describes
six events, Analytics then Fraud, and a fresh illustration at each loop boundary.
The remaining historical baseline was preserved. This resolves the missing old-control
sentence recorded in the previous deployment progress note without reintroducing
instructions for controls that no longer exist.

## Guidance and independent review

Updated `new-lesson`, `lesson-interactive` and its visual-storytelling reference, the
lesson-quality judge, review command and related authoring references. They now specify
automatic explanatory loops, purposeful use-case tabs, diverse representations,
visibility/reduced-motion behavior, original artwork with accurate provenance, readable
syntax, selective semantic emphasis and browser evidence. Authentic maintained asset
registries are permitted alongside official sources. The course-specific checker scope
and checklist keys are explicit; no new approval gate or arbitrary visual quota was added.

Independent reviewer `guidance_review` reported no required implementation or changed
VI/EN defects, checking routing/crash/CDC/retention frames, replica placement/election,
processed versus committed offsets, safe syntax rendering, and affected captions/callouts.
It identified checker-scope, storage-key and artwork-source guidance inconsistencies;
these were corrected, including both entrypoint summary bullets.

A fresh raw-fixture evaluator reported B PASS for supplied bilingual text/code, including
the justified 1,200 arithmetic correction and isolated independent-call cases. It reported
C INCOMPLETE because the referenced page 3 figure was unavailable, without inventing its
mechanism or claiming browser/i18n certification. The review agent then reached its usage
limit before an additional visual-plan exercise returned; that exercise is not counted.

The independent review covers changed content and implementation. It is not a fresh
full-PDF coverage audit or a current full-lesson translation gate. Historical fidelity
reports remain historical; no judged hash or full-lesson certification was manufactured.
