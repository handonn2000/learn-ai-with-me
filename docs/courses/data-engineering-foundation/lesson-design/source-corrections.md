# Source corrections and authoring checks

2026-09-07 · Applies to the six lessons in Parts III and IV.

Full coverage means accounting for each substantive claim, example, table, diagram, and code snippet. It does not mean presenting an incorrect claim as fact. Preserve the learning topic, explain a correction, and record the source page and evidence here. Confirmed items below have primary references; the remaining items are authoring checks, not completed technical validation.

Bao phủ đầy đủ nghĩa là xử lý từng khẳng định kiến thức, ví dụ, bảng, hình và đoạn code. Với chỗ sai, giữ chủ đề cần học, giải thích phần sửa và ghi trang nguồn cùng bằng chứng. Các mục đã xác minh có tài liệu chính thức; danh sách kiểm tiếp theo chưa phải kết luận đã thẩm định xong.

## Confirmed corrections / Các điểm đã xác minh

| Source | Required treatment | Evidence |
|---|---|---|
| L7 p24 | Kafka 4.0 removed ZooKeeper mode. Present the source diagram as an older architecture; do not say the replacement merely began with 4.0. / Kafka 4.0 đã bỏ chế độ ZooKeeper; sơ đồ nguồn thuộc kiến trúc cũ. | [Kafka upgrade guide](https://kafka.apache.org/42/getting-started/upgrade/) |
| L7 pp36–39 | Exclusive partition assignment and producer idempotence do not independently guarantee exactly-once external effects. Teach processing/offset ordering and the transactional boundary. Correct the repeated at-least-once heading. / Chia partition độc quyền và producer idempotence không tự bảo đảm tác động bên ngoài exactly-once; phải giải thích thứ tự xử lý/offset và ranh giới transaction. | [Kafka delivery semantics](https://kafka.apache.org/40/design/design/#message-delivery-semantics) |
| L10 pp31–37 | Firing and cleanup are different. Allowed lateness retains window state; subsequent eligible events may trigger another result. Cleanup follows event-time watermark progress, so the p37 drawing needs explicit watermarks, not only arrival-clock labels. / Trigger và dọn state khác nhau; allowed lateness giữ state và có thể phát kết quả lại. Timeline phải nêu watermark tại mỗi bước. | [Flink windows and allowed lateness](https://nightlies.apache.org/flink/flink-docs-release-1.20/docs/dev/datastream/operators/windows/#allowed-lateness) |
| L9 pp64,69–70 | Do not imply all Spark work automatically uses off-heap allocation. The documented `spark.memory.offHeap.enabled` default is false and explicit off-heap allocation requires a positive size. Separate binary execution improvements from this configuration. / Không đồng nhất mọi cải tiến Tungsten với off-heap được bật mặc định. | [Spark memory configuration](https://spark.apache.org/docs/latest/configuration#memory-management) |
| L11 p2, pp60–62 | The Feast practical is named but not developed. Add a labeled supplement: historical retrieval uses point-in-time joins; online serving retains latest feature values. A shared store alone does not prove all transformation logic, timestamps, and freshness are consistent. / Bổ sung Feast có nhãn: historical retrieval theo thời điểm, online store giữ giá trị mới nhất; vẫn cần kiểm logic/timestamp/freshness. | [Feast point-in-time joins](https://docs.feast.dev/getting-started/concepts/point-in-time-joins), [online store](https://docs.feast.dev/getting-started/components/online-store) |
| L12 p40 | The PHE incident involved 15,841 cases missing from daily reporting, subsequently transferred to contact tracing. People received their test results normally. Do not describe it as permanent loss of 16,000 test results. / Đây là chậm báo cáo 15.841 ca, không phải mất vĩnh viễn 16.000 kết quả xét nghiệm. | [PHE statement, October 2020](https://www.gov.uk/government/news/phe-statement-on-delayed-reporting-of-covid-19-cases) |

## Source inconsistencies and checks before writing runnable examples

These are a concrete verification backlog. Keep each original concept in the corresponding chapter while resolving its wording, version, or incomplete example.

| Source | Issue to resolve during implementation |
|---|---|
| L7 pp14,23,33,35,37,39 | Verify CDC source overhead and transaction/rollback behavior; remove unconditional no-loss promises. Explain partition throughput as workload-dependent, routing as partitioner/client-dependent, `acks=all` in relation to ISR, and failover durability with its actual assumptions. |
| L7 p43 | Preserve all ten database names in the connector comparison: MySQL, MariaDB, MongoDB, PostgreSQL, Oracle, SQL Server, Cassandra, Vitess, Spanner, Informix. Identify official/incubating/community/version support before suggesting executable connector setups. Distinguish Debezium source connectors from downstream sink connectors. |
| L8 pp9–15 | The storage/cache comparison contains broad performance, cost, scalability, and durability claims. Retain the comparison axes while adding workload/context. Verify HDFS block-size defaults for the chosen version instead of teaching 64 MB as timeless. Separate Kafka tiered storage from a separate historical-query sink. |
| L8 pp29–31,45,54–57 | Schema-on-read does not mean absence of metadata or contracts; warehouse capabilities vary; Avro is not itself a complete schema governance service. Bronze/Silver/Gold responsibilities are patterns, not an obligation that every silver table be 3NF or only minimally transformed. |
| L9 pp23,29–30,39 | Code is embedded in images. Recreate SparkSession/SparkContext, `parallelize`, `createDataFrame`, `toDF`, CSV/JSON/JDBC and serializer examples as readable code. Provide schema, dependencies, input fixture, expected output and dialect/version. Distinguish the JVM serializer from Python object serialization. |
| L9 pp45–53 | The first action computes and populates a lazy cache; it does not reuse data that has never been computed. Explain storage levels separately for JVM RDDs, Python RDDs and DataFrames. Do not copy JVM-only level names into runnable PySpark code. |
| L9 pp66–74 | Preserve the original SQL and both logical-plan diagrams. Explain legal filter movement and semantic equivalence; avoid promising that every operator is code-generated or that the illustrated cache latency ratio gives the same whole-job speedup. Verify AQE defaults for the selected version. |
| L9 pp79–88 | Salting must preserve the aggregate or join result, including the final merge and any corresponding join-side expansion. Validate approximate-count tradeoffs, dynamic allocation requirements/defaults, and compaction operations. Logical data preservation does not guarantee identical physical byte size. |
| L10 pp16–22,32–40 | Explain half-open window boundaries and timestamp precision, disorder allowance versus allowed lateness, and the watermark of multiple inputs. Monotonic-watermark strategy alone does not mean every out-of-order event is dropped. A p95 delay is not a maximum delay guarantee. Side-output pipelines also consume resources. |
| L10 pp41–50 | The ProcessFunction agenda item needs a labeled explanatory bridge. Image code on p46 groups by `window_start` without defining a window. Supply the missing window and connector setup. Explain legacy group-window SQL versus the chosen runnable syntax; do not promise identical plans for arbitrary DataStream and Table API programs. |
| L10 pp51–61 | Window state, exactly-once and state scaling/consistency are named without full treatment. Add definitions and bounded examples. Verify PyFlink operator-state support for the selected version. Explain multi-input checkpoint coordination and source/sink requirements without hiding them behind the checkpoint animation. |
| L10 pp64–73 | Distinguish chaining, slot sharing and resource isolation. Backpressure can appear upstream of its cause; inspect the downstream bottleneck. Async I/O and state-cache changes depend on the actual bottleneck. Session versus application deployment is a tradeoff, not a universal production prohibition. |
| L11 pp9–15 | Preserve candidate/composite-key and functional-dependency reasoning. The 2NF example changes quantity from 1 to 2 while decomposing a row; choose a consistent fixture and note that normalization must not alter values. Repeated order IDs can be valid at order-line grain. |
| L11 pp28–34 | Type 1 is described as rarely useful on p29 and common on p34. Teach overwrite as a policy for corrections or attributes without required history. Distinguish natural business key from the primary key of a versioned dimension; explain active flags versus deletion. Identify the source's separate-history-table meaning of Type 4, since taxonomies differ. |
| L11 pp40,44–54 | Kimball integration needs conformed dimensions, not an arbitrary union of unrelated marts. Vectorized execution is broader than a universal one-instruction claim. Verify ClickHouse Keeper/ZooKeeper and storage layout, and Pinot hybrid-table time boundaries. Qualify engine speed, freshness and update claims by workload/configuration. |
| L11 pp56–62 | Rebuild the transaction JSON with valid syntax. Feature freshness numbers and sub-10ms lookup expectations are scenario choices. Show point-in-time correctness, consistent filters/null handling, and refresh lag separately; include the original 45.73/46/47.10 examples as illustrative discrepancies. |
| L12 p15 | Idempotency, backfill and dynamic pipelines are named in the agenda but lack developed sections. Add sourced, labeled bridges including rerun-safe writes and explicit data intervals. |
| L12 pp23–37 | Pin Airflow/core/provider versions before producing executable examples. Label older metadata-database architecture; compare it with the selected version's task execution model. Distinguish TaskFlow `@task` from an explicit PythonOperator. Correct the FileSensor caption: the shown default waits for presence. Check operator/executor availability, `max_active_runs` spelling and concurrency scope. |
| L12 p40 | The Gartner $11.8M and Unity $5B statements need primary provenance and date/context. Preserve them in this audit as unverified source claims. Do not silently teach those numbers as established facts or imply market-cap movement is a measured direct data-loss cost. |
| L12 pp42–61 | A syntactically valid value need not be accurate. Example rules depend on the business contract (including future timestamps and legitimate zero-price orders). Distinguish failed tasks, branch choices, skipped tasks and downstream trigger rules. Validate GX/Deequ/provider APIs; separate rule execution from catalog/lineage functions in DataHub. |

## Acceptance of a correction

For each resolved item, record the lesson anchor, the source page, the tested version or reference, and the exact scope of the change. Update both languages and any affected simulation. Unresolved claims remain visibly qualified in the authoring audit; the final coverage report must disclose them.

Với mỗi mục đã xử lý, ghi anchor bài, trang nguồn, phiên bản đã chạy hoặc tài liệu, cùng phạm vi sửa. Cập nhật cả hai ngôn ngữ và simulation liên quan. Báo cáo coverage cuối phải nêu các khẳng định chưa xác minh xong.
