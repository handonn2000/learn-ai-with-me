import type { VI } from './lesson12.text.vi';

export const EN = {
  badge: 'LESSON 12', title: 'Orchestration & data quality', cover: 'Cover',
  eyebrow: 'DATA ENGINEERING FOUNDATION / PART IV / 12',
  hero: 'On time.<br/>But is the <em class="hl-cyan">data right?</em>',
  intro: '02:00: the pipeline starts extracting orders. 03:00: transformation starts on schedule. The last file arrives at 03:20. Every clock kept its promise; the revenue report is wrong. I’ll rewind this shift so you can see when to run, when to wait, and when to stop the data.',
  meta: '10 chapters · 3 interactive labs · 10 knowledge checks',
  start: 'Start the shift', jump: 'Try the quality gate',
  premise: 'The retail scenario and labs are teaching simulations; they don’t connect to a database or run Airflow. API examples use Airflow 3.1.0 as a reference; older architecture is labeled.',
  readyTitle: '🧭 Before the control room', readyLead: 'You don’t need advanced graph theory. Keep these six ideas in mind to distinguish a diagram from an actual execution.',
  terms: [
    ['Directed graph', 'A → B means B depends on A. A DAG arrow expresses an execution condition; it doesn’t automatically move data.'],
    ['Acyclic', 'A → B → C is valid; adding C → A creates a waiting cycle. A DAG has no cycles; retrying a task doesn’t add a backward edge.'],
    ['Task state', 'queued, running, success, and failed describe a task instance. You need its state to decide whether downstream work is eligible.'],
    ['Retry', 'Another attempt after an error in the same task instance. It helps only if the error can clear; retrying doesn’t automatically repair bad data.'],
    ['Data interval', 'The [start, end) range a run processes. Include the start, exclude the end, so a boundary record isn’t counted twice.'],
    ['Validation rule', 'A machine-checkable condition, such as a nonempty order_id. A rule needs a threshold, evidence, and an action when it fails.'],
  ],
  chapters: [
    {
      title: 'Three correct clocks, one wrong report', lead: 'Transform doesn’t know that extraction is still running. An appointment on a calendar doesn’t supply that information.',
      body: [
        'A pipeline often follows <strong class="hl-cyan">extract → transform → load</strong>: fetch source data, clean it using business rules, put the results in a warehouse, then refresh a dashboard or serve ML. A 02:00 / 03:00 / 04:00 schedule assumes that sources are available and earlier steps always succeed and finish on time.',
        'Extraction runs until 03:20, but transformation starts at 03:00 and reads partial input. Loading at 04:00 can still return a success code and write a report with missing orders. <strong>A successful command doesn’t prove its input was complete.</strong>',
        '<strong class="hl-cyan">Orchestration</strong> coordinates, schedules, and manages execution: resolve dependencies, track states in metadata, apply retries, and notify on failure. Python, SQL, Spark, or another processing system still performs the heavy computation.',
        'I put extract_orders and extract_customers in parallel because they’re independent. Transform becomes eligible only after both succeed; load waits for transform; the dashboard waits for load. Coordination blocks work where necessary and permits parallel work where possible. Try the two scenarios below: the clock says 03:00 in both, but the decisions differ.',
      ], headers: ['Responsibility', 'The orchestrator’s decision'], rows: [['Dependency', 'What must finish first, and what is independent?'], ['Scheduling', 'Which schedule or signal creates a run?'], ['State', 'What state is this run’s task in?'], ['Fault tolerance', 'Retry, block downstream work, and notify whom?']],
    },
    {
      title: 'A run needs a memory', lead: 'The next morning, you need to know which step failed and which attempt wrote data. Scattered logs can’t reconstruct the whole workflow on their own.',
      body: [
        'A run can start on a timer, from a manual action, when data becomes available, or on an external event. Examples include a daily 02:00 run, rerunning yesterday’s pipeline, waiting for an S3 object, and receiving a new-batch upload notification. <strong>Scheduling creates the run; orchestration determines how its tasks execute.</strong>',
        'The scheduler considers a task once its dependencies are met, queues it, and a worker executes it. Success opens the way for downstream work; a transient failure may lead to another attempt after a delay. Airflow calls the waiting-for-retry state <code>up_for_retry</code>. Exhausted attempts lead to failed; downstream tasks using all_success are blocked and may become upstream_failed.',
        'Skipped means a branch was deliberately bypassed; it isn’t the same as failed or success. I keep task ID, run ID, data interval, attempt number, and logs together. If the failure persists, notify the owner, retain evidence, and allow a rerun after repair.',
        'Bounded retries, delays, and backoff avoid piling requests onto an unhealthy source. A wrong password or changed schema needs a configuration or contract fix, not 100 blind attempts. Even 100 successful tasks querying the same database can slow the whole system; chapter 06 will put limits around them.',
      ], headers: ['State', 'Meaning and next step'], rows: [['queued', 'Assigned to the executor, awaiting a worker.'], ['running', 'A worker is executing the task.'], ['success', 'Execution finished; downstream conditions still apply.'], ['up_for_retry', 'Waiting for another attempt under the retry policy.'], ['failed / upstream_failed', 'The task failed / an upstream dependency prevents execution.'], ['skipped', 'The branch wasn’t selected, or the task deliberately skipped.']],
    },
    {
      title: 'A timer can’t judge readiness', lead: 'Moving transform back an hour only postpones the risk until extraction takes another hour longer.',
      body: [
        'Cron runs scripts using a time expression; <code>0 2 * * *</code> means daily at 02:00 in the schedule’s timezone. It fits an independent job that only needs a timer. Cron has no built-in DAG, dependencies, centralized run metadata, or workflow recovery. A script can chain commands, check failures, and write logs, but you’re then building the coordination layer yourself.',
        'A conventional crontab line has 5 time fields followed by the command: minute 0–59, hour 0–23, day of month 1–31, month 1–12, and day of week 0–7 (Sunday is 0 or 7 in common Unix cron). A * accepts every value in that field; 0 2 * * * selects minute 0, hour 2, every day. Other systems use different dialects, so check the scheduler before moving expressions.',
        '<strong class="hl-purple">Supplement for topics named only in the agenda:</strong> idempotency, backfill, and dynamic pipelines need a concrete example. Idempotency means repeating the same input and interval doesn’t introduce additional unintended effects. Appending with INSERT can duplicate rows; an upsert by key or transactional partition replacement can be safe when designed correctly.',
        'Retry attempts a failed task again. Rerun executes an existing task or run again. Backfill processes selected historical intervals. A run for [2026-05-15, 2026-05-16) must read that range even if it executes on another day. With data-interval scheduling, a run is generally scheduled after its interval ends; its logical date isn’t the worker’s start time.',
        'Dynamic DAG generation builds structure from configuration; dynamic task mapping creates task instances from a runtime input list. A list of 3 partitions produces 3 processing tasks, not an infinite DAG loop. Keep IDs and ordering stable, avoid unnecessary API/database calls while parsing files, and use pools to limit load.',
        'In the lab, publish the same interval twice. I use partition replacement with a fixed fixture: the total stays at 3 rows. That illustrates idempotency; a real system also needs transactions, write coordination, and a policy for changing historical input.',
      ], headers: ['Situation', 'Deliberate action'], rows: [['Temporary API timeout', 'Retry the task while preserving its data interval.'], ['Fix logic for earlier days', 'Backfill a specific range, control load, and reconcile results.'], ['Receive a changing file list', 'Map tasks over a finite list.'], ['Rerun the same batch', 'Write idempotently by partition/key; don’t append blindly.']],
    },
    {
      title: 'A DAG is a plan; a run is an event', lead: 'The same extract_orders task appears every day. Its name alone doesn’t identify a particular execution.',
      body: [
        'Airflow represents a workflow as a DAG: tasks connected by directed dependencies without cycles. The DAG <code>daily_sales_pipeline</code> is a definition; a DAG Run for 2026-05-15 is one execution; the task <code>extract_orders</code> is a step; a task instance is that step within a particular run (and map index when mapped). A retry creates another attempt of this instance.',
        'Airflow supports workflows as code, scheduling, state inspection, history, and retries. It <strong>isn’t a database, warehouse, message queue, or streaming engine</strong>. A task can execute Python or submit a Spark/dbt/Docker job; Airflow doesn’t become a distributed computation engine or dataset store.',
        'In older Airflow diagrams, DAG files supply the plan; the scheduler identifies eligible tasks; the executor selects the execution mechanism; workers run code; the metadata database holds runs, states, retries, and configuration; the webserver/UI exposes status, logs, and manual actions. Those diagrams show components communicating directly through the database.',
        '<strong class="hl-yellow">Airflow 3.1.0 reference:</strong> a DAG processor parses and serializes definitions; the scheduler coordinates execution through the executor. Task code communicates through the Task SDK and Execution API served by the API server, rather than accessing the metadata database directly. The API server also serves the UI. For deferrable tasks, the triggerer monitors waiting conditions and wakes tasks; it doesn’t perform the entire transformation.',
        'The diagram below shows control and state paths. Orders live in external storage; arrows in an architecture diagram don’t send the dataset through the metadata database.',
      ], headers: ['Concept', 'Example'], rows: [['DAG', 'daily_sales_pipeline'], ['DAG Run', 'Run for interval 2026-05-15'], ['Task', 'extract_orders'], ['Task Instance', 'extract_orders within the 2026-05-15 run']],
    },
    {
      title: 'Pass the address, not the warehouse', lead: 'Two tasks may execute on different machines. A Python variable or /tmp file from one task doesn’t automatically appear in another.',
      body: [
        'An operator is a template for work; instantiate one in a DAG to create a task. <code>PythonOperator</code> invokes a Python function; <code>@task</code> is the more convenient TaskFlow declaration. Calling a decorated function while building the DAG creates a dependency/XCom reference rather than immediately running the processing function on the scheduler.',
        'A sensor is an operator that waits for a condition: a file/S3 object, an updated table, an available API, or another completed workflow. <code>FileSensor</code> waits for a path to be present by default, not for it to disappear. Presence doesn’t prove a file is fully written; prefer a completion marker or the source’s commit mechanism. The worker must be able to see the path. Reschedule mode releases the worker slot between checks; deferral needs operator support and a triggerer.',
        '<strong class="hl-cyan">XCom</strong> lets tasks push/pull small values: object paths, row counts, IDs, or model URIs. The default backend stores values in the metadata database; don’t send a large DataFrame. An object-storage backend can change where payloads live, but size, access, and lifecycle still need management.',
        'For example, training pushes <code>model_path</code> using <code>xcom_push</code>, and evaluation pulls the same key and task ID. If the value is /models/v2, it must be a shared path or accessible URI, not a local directory with a coincidentally matching name. TaskFlow creates XComs automatically for return values; dependencies don’t transfer datasets between machines.',
        'I’ve reconstructed the Bash, TaskFlow, FileSensor, and Docker examples below as readable code. These are declarations inside a DAG, not a deployed Airflow system. Docker needs an existing image, a valid daemon/connection, and mounts on the daemon’s host; the provider determines parameter signatures.',
      ], headers: ['Operator / API', 'Work performed'], rows: [['PythonOperator / @task', 'Execute a Python function / declare a TaskFlow task.'], ['BashOperator', 'Run a shell, CLI, or dbt command.'], ['SQLExecuteQueryOperator', 'Execute SQL through a connection; replaces the older PostgresOperator name in the source.'], ['S3KeySensor / FileSensor', 'Wait for an object / path to be ready.'], ['DockerOperator', 'Execute work in a container.'], ['HttpOperator', 'Call an HTTP API or webhook.'], ['EmailOperator', 'Send a notification after configuring its provider/connection.']],
    },
    {
      title: 'Protect the database beneath the scheduler', lead: '200 individually correct tasks can still overload one PostgreSQL database together.',
      body: [
        'Variables hold configuration such as bucket names, date ranges, or feature flags. Connections hold database/API/cloud endpoints and credentials; tasks use connection IDs instead of passwords pasted into DAGs. The UI, environment variables, and secrets backends can supply values; access control, encryption, and log redaction still need configuration. I don’t treat moving a password out of code as the end of security work.',
        'The executor determines how tasks run: LocalExecutor uses processes on one machine; CeleryExecutor distributes work to workers through a broker; KubernetesExecutor creates a pod per task. SequentialExecutor, which ran one task at a time, is historical: it was removed in Airflow 3.0 and isn’t a choice for 3.1.0. Each option changes operating cost, isolation, and scaling.',
        '<code>parallelism=64</code> limits concurrently running task instances per scheduler in the Airflow 3.1.0 configuration; this example assumes one scheduler. Don’t interpret 64 as an absolute ceiling for every multi-scheduler deployment. Worker capacity, the executor, and DAG limits can reduce actual concurrency.',
        '<code>max_active_runs=1</code> limits active runs of the same DAG; it doesn’t serialize tasks within a run or lock other DAGs. The correct spelling is max_active_runs, not max_activate_runs. <code>max_active_tasks</code> is a separate task limit at DAG level.',
        'A <code>postgres_pool</code> with 5 slots permits at most 5 one-slot tasks using that pool at once. Heavy tasks can request multiple slots through pool_slots. With 200 ready database tasks, a ceiling of 64, and a pool of 5, the bottleneck is 5: increasing 64 doesn’t expand the database. A pool is a logical quota; it doesn’t create workers or connections.',
        'Increase the run limit in the lab, then shrink the pool. Watch the waiting categories, not just the task total. In operations, use Grid/Graph and logs to locate failing instances; send contextual email/Slack alerts; use Grafana/Datadog for queue depth, durations, retries, and data-lag trends. Green tasks still aren’t enough: the next chapter checks the data itself.',
      ], headers: ['Executor', 'Mechanism / tradeoff'], rows: [['SequentialExecutor · historical', 'One task at a time; removed in Airflow 3.0.'], ['LocalExecutor', 'Multiple local processes; bounded by machine resources.'], ['CeleryExecutor', 'Worker cluster and broker; distributed infrastructure to operate.'], ['KubernetesExecutor', 'A pod per task; isolation with startup and cluster-operation overhead.']],
    },
    {
      title: 'Green tasks can still carry bad data', lead: 'A price column full of valid integers can still contain yesterday’s prices.',
      body: [
        'Good data is <strong class="hl-cyan">fit for its intended use</strong>. Missing required values, wrong types or formats, duplicates, stale data, disagreement across systems, and unexpected schema changes can all distort decisions. Correct syntax doesn’t establish real-world correctness.',
        'A real incident: in 2020, Public Health England confirmed that 15,841 cases were delayed in daily reporting and transfer to contact tracing; people still received their test results normally. This wasn’t the permanent loss of 16,000 results. I keep that distinction because timeliness and data loss are different failures.',
        'The source’s $11.8M figure attributed to Gartner and $5B Unity market-cap figure lack sufficient primary provenance/context here to teach them as confirmed direct costs. They aren’t reference statistics in this lesson. A market-cap decline also doesn’t establish that one data error caused the entire decline.',
        'Turn dimensions into rules: customer_id isn’t NULL; order_id is unique; email matches its format; price > 0; event_time isn’t in the future; daily row count drops no more than 30% against a suitable baseline. This is an example contract: free orders may be valid, timestamps may need clock-skew tolerance, and holidays need a different baseline. I want you to choose rules from the business need, not copy appealing thresholds.',
      ], headers: ['Dimension', 'Concrete check', 'Limit'], rows: [['Accuracy', 'A price of 100 matches the actual price of 100.', 'Needs a trusted reconciliation source.'], ['Completeness', 'No missing email if the contract requires it.', 'Doesn’t establish email correctness.'], ['Consistency', 'The same user ID agrees across systems.', 'Systems can share the same error.'], ['Timeliness', 'Data is available within a 5-minute cycle.', 'The use case determines the threshold.'], ['Validity', 'Phone matches the chosen market’s 10-digit format.', 'A valid format doesn’t prove reachability.'], ['Uniqueness', 'No duplicate user_id at one-row-per-customer grain.', 'Order lines can legitimately repeat order_id.']],
    },
    {
      title: 'Quality improves through feedback', lead: 'You repair a CRM file today; tomorrow the input form creates the same errors again.',
      body: [
        'Start with purpose: the retailer needs to contact customers. Profile data before defining rules, monitoring results, and repairing the source. In this example, 15% of records lack email and 10% have invalid formats; there are also duplicate customers and inconsistent phone formats. State the denominator for each rate; don’t add rates without knowing whether the groups overlap.',
        'The contact contract requires at least one valid email or phone number. Standardize phones using country codes and market rules; +84-555-1234 and 05551234 illustrate different formats, not proof that both numbers are valid or identify the same person. An email regex screens format; deliverability needs separate verification.',
        'Customer IDs must be unique; name + date of birth is a duplicate-flagging heuristic, not sufficient grounds to merge two people automatically. I retain a review step and old-ID mappings during deduplication. Alert the owner when invalid emails exceed 5%; track each rule’s weekly violation rate on a dashboard. Repair records, standardize phones, verify emails, and train sales/marketing staff so the next cycle produces fewer errors.',
        'Monitoring needs four capabilities: coverage at table/row/column level; alerts to the right people with rule, expected/observed values, run, and interval; a fast resolution path with an accountable owner; and reusable rules/metadata that scale across datasets. Don’t put raw personal data in an alert when an evidence ID will do.',
      ], headers: ['Detection approach', 'Strength / limitation'], rows: [['Manual', 'Users happen to spot mistakes; limited system-wide coverage.'], ['Rule-based', 'Explainable null, uniqueness, and range checks; catches only specified conditions.'], ['Metric monitoring', 'Tracks row count, freshness, and distributions; needs baselines and seasonality.'], ['Machine learning', 'Detects complex changes; false positives require tuning and investigation.']],
    },
    {
      title: 'A check must change execution', lead: 'Printing “validation failed” and returning normally leaves the task green. A warning doesn’t automatically block loading.',
      body: [
        'The dangerous path is ingest → validation reports failure → transform → load → dashboard/ML. Bad data gets published before anyone reads the logs. <strong class="hl-red">A validation gate must turn check results into an execution condition.</strong>',
        'The simple option: validation raises an error on bad data; transform and load depend on it with the all_success trigger rule. Alert through a callback or an appropriate one_failed branch. Don’t put all_done on publication, since it can run after upstream failure. A successful final all_done cleanup task can also make the DAG Run appear successful; monitor the gate task and publication condition separately.',
        'The branching option uses BranchPythonOperator or TaskFlow @task.branch. Validation finishes and returns a small result; a branch selects transform or alert/quarantine. The other branch is skipped. A successful branch task only means it selected a path, not that the data passed. If branches rejoin, choose the appropriate trigger rule while keeping publication downstream of the validated path.',
        'I call this a data circuit breaker: cut off publication on failure, preserve evidence, and notify the owner. It needn’t implement every closed/open/half-open state of a network-service circuit breaker. Quarantine keeps the bad batch separate; the dashboard retains the last validated version with freshness information.',
        'Choose a fault in the lab and inspect the failing rule and blocked consumers. After repairing the input, validate the same interval again; publish only once it passes. Repeatedly retrying the original bad batch doesn’t repair a null key or renamed schema.',
      ], headers: ['Component', 'Decision'], rows: [['Validation task', 'Evaluate rules; retain expected, observed, run, batch, and evidence location.'], ['Fail / branch', 'Block all_success or select a safe path.'], ['Alert / quarantine', 'Notify the owner; preserve the batch for investigation.'], ['Transform → load → consumers', 'Use validated output for the correct data interval only.']],
    },
    {
      title: 'Make the agreement executable', lead: 'Upstream renames user_id to userId. Nobody updates downstream SQL because nobody knows an interface just changed.',
      body: [
        'A <strong class="hl-cyan">data contract</strong> is an agreement between producer and consumer: column names/types, nullability, ranges/formats, freshness, uniqueness, and change responsibilities. Renaming user_id breaks queries; removing purchase_date breaks a feature pipeline; changing a type can corrupt calculations or model inputs; allowing NULL in a required field leaves holes in a dashboard.',
        'An example contract requires INTEGER user_id, non-null purchase_date, age from 0 to 120, data updated within 24 hours, and unique transaction_id. Store it in a machine-readable form, version it, validate at team boundaries, and notify both sides on violation. An agreement doesn’t prevent every change: compatibility, version transitions, and migration need a process.',
        'GX calls a rule an Expectation; an Expectation Suite collects rules. The names expect_column_values_to_not_be_null, expect_column_values_to_be_unique, expect_column_values_to_be_between, and expect_column_values_to_match_regex describe four check families. Newer GX Core uses corresponding Expectation classes; don’t mix old syntax with new APIs. Backends cover file data through suitable engines, SQL/warehouses, and Spark; supported types/expectations vary by backend.',
        'The GX flow in Airflow is load data → run validation → inspect success → fail or branch → transform or alert. A success=false result must become a blocking decision in the task; writing a GX report alone isn’t sufficient. A suite expresses a contract’s rules, not automatically all its ownership, freshness SLA, or compatibility policy.',
        'Deequ runs on Spark: profiling discovers dataset characteristics, analyzers compute size/completeness/uniqueness/type/distinct metrics, and VerificationSuite checks constraints. For example, <strong>completeness > 99%</strong> is strict: 99/100 fails, 100/100 passes. Anomaly detection compares metrics across batches; incremental metric computation reuses suitable state to avoid full recomputation, but not every metric can be added arbitrarily.',
        'DataHub manages dataset metadata, schemas, owners, pipelines, lineage, and quality results. When orders fail, lineage identifies the affected sales_daily table, dashboard, and features/model. A catalog doesn’t replace GX/Deequ to validate every row automatically; Airflow blocking still needs explicit wiring. I use validation to identify what failed and lineage to identify who is affected.',
      ], headers: ['Tool', 'Primary role'], rows: [['Great Expectations', 'Expectation suites and validation on supported data backends.'], ['Deequ', 'Spark profiling, metrics, and constraints; anomaly and incremental state.'], ['dbt tests', 'SQL model checks: not_null, unique, relationships, accepted_values.'], ['Soda Core', 'Declarative quality rules; scans against supported sources.'], ['DataHub', 'Catalog, ownership, lineage, and quality visibility.']],
    },
  ],
  architecture: { title: 'Coordination paths · Airflow 3.1.0', plan: 'DAG files → DAG processor → serialized plan', dispatch: 'Scheduler → Executor → Worker / Task SDK', state: 'Task SDK ↔ Execution API ↔ Metadata DB', ui: 'UI ↔ API server ↔ Metadata DB', data: 'Worker ↔ Object storage / Warehouse', note: '→ control direction; ↔ state or data exchange as labeled. The storage path carries datasets; metadata holds state and small values.' },
  code: { title: '⌨ From image to code', lead: 'Declaration examples: Python 3.11, Airflow 3.1.0, standard provider 1.9.0; Docker illustrates provider 4.4.4. Read the snippets inside a DAG; real connections, files, and images must be prepared.', operatorNote: 'Bash prints 1; @task declares a task; FileSensor waits for a file to appear. Timeout prevents waiting forever. Docker maps host directories into /training/code and /training/data, then runs train.py inside the container. docker_url selects a local daemon through its Unix socket; docker_conn_id is a registry login connection, not the daemon address. auto_remove="success" uses the selected provider’s string form, unlike True in the older example.', gateTitle: 'A small gate with a clear result', gateLead: 'This complete TaskFlow dependency example uses a tiny fixture in XCom for teaching; production stores the batch externally and passes a URI. It has no warehouse write side effect. A null key makes validate fail and blocks publish; change None to 102 and rerun to return count=2, total=200000.', contractTitle: 'An illustrative contract, not a DataHub configuration schema', xcomTitle: 'XCom keeps only the reference', xcomNote: 'These functions belong to separate tasks. The evaluation worker must be able to access the shared URI; key and task_ids identify the right value within the same run.' },
  labs: {
    demo: 'SIMULATION / DOES NOT RUN AIRFLOW',
    schedulerTitle: '⏱ Clock or dependency?', schedulerHelp: 'Switch scenarios and watch transform at 03:00. The table retains the whole story even with motion disabled.', modes: ['Cron only', 'Dependency-aware'],
    timelineHeaders: ['Time', 'Extract', 'Transform / outcome'],
    timeline: [['New illustration cycle', 'Not started', 'Not started'], ['02:00', 'Running', 'Waiting'], ['03:00', 'Unfinished', 'Reads partial input'], ['03:20', 'Success', 'Result already lacks data'], ['04:00', 'Success', 'Publishes a wrong report']],
    safeTimeline: [['New illustration cycle', 'Not started', 'Not started'], ['02:00', 'Running', 'Waiting'], ['03:00', 'Unfinished', 'Blocked: extract hasn’t succeeded'], ['03:20', 'Success', 'Dependencies met → start transform'], ['04:00', 'Success', 'Publish after transform succeeds']],
    timelineNote: 'Assume safe transformation takes 20 minutes and loading takes 20 minutes; all steps succeed. Dependencies alone don’t guarantee deadlines or quality.',
    interval: 'Data interval', publish: 'Publish / rerun interval', rows: 'Published rows', attempts: 'Publication attempts', idempotent: 'Each interval contains 3 rows; reruns replace that partition. Choose another interval to simulate backfill.',
    poolTitle: '🎛 Resource control room', poolHelp: '2 runs × 100 independent database tasks; each holds 1 slot and completes after 1 tick. Assume one scheduler, enough workers, and no additional DAG task limits.',
    capacity: 'parallelism', runs: 'max_active_runs', slots: 'postgres_pool · slots', tick: 'Complete running batch', reset: 'Reset experiment', running: 'Running', waiting: 'Awaiting resources', blocked: 'Awaiting run admission', done: 'Completed', poolNote: 'Changing a limit resets the experiment. Slots are logical quotas; highlighted cells are tasks holding slots, not physical workers.', run: 'Run', poolEvidence: 'Tasks running in pool',
    gateTitle: '🛑 Stop errors before publication', gateHelp: 'Choose a fault, validate the batch, then repair and recheck it. The same batch snapshot must pass before publication.',
    faults: ['Valid batch', 'Null customer_id', 'Duplicate order_id', 'Negative amount', 'Stale dataset', 'Renamed schema', 'Row-count drop', 'Future timestamp'],
    check: 'Validate batch', fix: 'Repair input & recheck', export: 'Publish validated batch', unchecked: 'Unchecked', pass: 'Pass', fail: 'Fail · stop & notify owner', published: 'Published', quarantined: 'Quarantine batch · retain previous good version', pending: 'Awaiting validation of current batch', consumerBlocked: 'Consumer blocked', consumerReady: 'Consumer ready',
    rules: ['Required schema', 'Non-null customer_id', 'Unique order_id', 'amount > 0', 'Freshness ≤ 24 hours', 'Row-count drop ≤ 30%', 'event_time ≤ check time'],
    ruleHeaders: ['Rule', 'Observed', 'Result'], evidence: 'Evidence', expected: 'Example contract threshold',
    lineage: ['orders / producer: retail', 'sales_daily / owner: analytics', 'dashboard / owner: BI', 'features / owner: ML'],
    gateNote: 'Lab rules are illustrative JavaScript, not GX/Deequ. The snapshot and check time are fixed for repeatable results. Don’t “repair” production by deleting every bad row automatically.',
  },
  summaryTitle: '📌 The map at the end of the shift',
  summary: ['Schedules create runs; dependencies, state, and resources determine task eligibility.', 'A task instance belongs to a run; retries preserve the data interval being processed.', 'Reruns/backfills need idempotent writes; XCom carries small metadata and accessible URIs.', 'parallelism, max_active_runs, and pools constrain three different things.', 'Quality depends on intended use; validity doesn’t establish accuracy.', 'Validation gates publication; contracts define expectations; lineage reveals impact.'],
  checkTitle: '✓ Check before leaving',
  checks: ['I can explain why a 02:00 / 03:00 / 04:00 schedule can publish bad data.', 'I can distinguish DAG, DAG Run, Task, Task Instance, and attempt.', 'I can design interval-based reruns/backfills without duplicating results.', 'I can explain how a pool of 5 limits database pressure even with parallelism at 64.', 'I can identify the rule, evidence, owner, and affected consumers when a gate fails.', 'I can distinguish GX, Deequ, and DataHub; I don’t equate green tasks with correct data.'],
  quizTitle: 'End-of-lesson quiz',
  topicLabels: { 'def12.dependencies': 'Dependencies and state', 'def12.reruns': 'Retries and backfills', 'def12.airflow': 'Airflow and XCom', 'def12.resources': 'Concurrency and pools', 'def12.quality': 'Quality and contracts', 'def12.gates': 'Validation gates and lineage' },
  quiz: [
    { topicId: 'def12.dependencies', q: 'Extraction finishes at 03:20. Transform has a 03:00 cron schedule. Which fix addresses the cause?', opts: ['Add workers', 'Make transform depend on successful extraction', 'Move cron back 5 minutes', 'Disable logs'], a: 1, ex: 'Dependencies use actual execution results; a fixed buffer doesn’t establish input readiness.' },
    { topicId: 'def12.dependencies', q: 'What is extract_orders within the 2026-05-15 run?', opts: ['A new DAG', 'An executor', 'A task instance', 'A connection'], a: 2, ex: 'A task is a definition; an instance associates it with a run (and a map index when applicable).' },
    { topicId: 'def12.reruns', q: 'Which property prevents duplicated data when rerunning the same partition?', opts: ['Idempotency', 'Append on every attempt', 'Change logical date to the current time', 'Retry indefinitely'], a: 0, ex: 'An upsert or partition replacement with appropriate transactions prevents additional unintended effects from identical input.' },
    { topicId: 'def12.airflow', q: 'Which payload fits the default XCom backend?', opts: ['A very large DataFrame', 'An entire video file', 'A whole warehouse table', 'An object URI and row count'], a: 3, ex: 'XCom carries small metadata. Datasets live in storage accessible to the workers.' },
    { topicId: 'def12.resources', q: '200 ready one-slot tasks, one scheduler with parallelism=64, pool=5, sufficient workers. How many pool tasks can run together?', opts: ['64', '200', '5', '1'], a: 2, ex: 'The pool of 5 is the tightest limit under these assumptions; waiting tasks don’t consume extra slots.' },
    { topicId: 'def12.resources', q: 'What does max_active_runs=1 guarantee?', opts: ['All tasks in the run execute sequentially', 'At most one active run of this DAG under this limit', 'Every DAG shares one database lock', 'There is only one worker'], a: 1, ex: 'Limiting runs isn’t the same as limiting tasks; independent tasks within a run may still execute in parallel.' },
    { topicId: 'def12.quality', q: 'An email matches the regex but belongs to someone else. What may still be wrong?', opts: ['Accuracy', 'The DAG name', 'Slot count', 'Acyclicity'], a: 0, ex: 'Validity checks format/rules; accuracy needs real-world verification. A regex doesn’t establish identity or deliverability.' },
    { topicId: 'def12.gates', q: 'Validation logs an error, returns normally, and all_success publication still runs. What fixes it?', opts: ['Hide the logs', 'Change publication to all_done', 'Only send an email', 'Raise an error or branch so publication requires a pass'], a: 3, ex: 'Rule results must change execution conditions. An alert doesn’t automatically block downstream work.' },
    { topicId: 'def12.quality', q: 'A Deequ rule requires completeness > 99%. Do 99 present values out of 100 rows pass?', opts: ['Yes, round to 100%', 'No: it equals 99%, rather than exceeding it', 'Yes, if the task is green', 'The ratio cannot be calculated'], a: 1, ex: 'The > comparison is strict. 99/100 = 99%, which isn’t greater than 99%.' },
    { topicId: 'def12.gates', q: 'Orders violate a contract. What does lineage add?', opts: ['Automatically repair every bad value', 'Automatically deploy Airflow', 'Identify affected dashboards/models and owners', 'Bypass validation'], a: 2, ex: 'Validation identifies the failure; lineage identifies origins and impact. Blocking and repair still require a workflow and an owner.' },
  ],
  practiceTitle: '🔧 Practice: hand over a trustworthy pipeline',
  practice: ['Draw extract_orders + extract_customers → validate → transform → publish → dashboard. Label dependencies and where the actual datasets live.', 'Choose [2026-05-15, 2026-05-16) and explain retry versus backfill. Show that writing the same partition twice doesn’t increase row count.', 'For 200 database tasks, choose parallelism=64, max_active_runs=1, pool=5. Record running tasks, tasks awaiting resources, and the held-back run.', 'Write a contract for schema, nulls, uniqueness, amount, and freshness. Inject a bad batch and record rule, expected/observed values, owner, quarantine URI, and blocked consumers.', 'Repair the input, revalidate, publish, and reconcile. Explain the difference between successful execution, passing data, and a published dataset.'],
  practiceAnswer: 'Open the review criteria',
  practiceGuide: 'The two extracts are independent; validate waits for both. A failed gate blocks publication, and the dashboard retains the previous good version. Use an immutable batch URI and transactional partition/key writes. Initially in the lab: 5 tasks run, 95 await resources, and 100 await the second run; rerunning an interval doesn’t change row count. A contract needs ownership and a version-change process; validation evidence must refer to the snapshot actually published.',
  next: 'You’ve reached the coordination layer of the data platform. Your deliverable is a design whose waiting, failure, recovery, and protection against bad data you can explain.',
} satisfies typeof VI;
